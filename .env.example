# ThoughtProof Sentinel API key (Phase 0 auth is open; key still recommended).
SENTINEL_API_KEY=

# Optional: override the endpoint (defaults to production).
# SENTINEL_URL=https://sentinel.thoughtproof.ai/sentinel/verify

# Signing mode: off (default) | dryrun | live
MM_EXECUTION_MODE=off
